#include<stdio.h>
int main(){
	int *pnum;
	char *pch;
	float *pfnum;
	double *pdnum;
	long *plnum;
	printf("size of integer is %d\n",sizeof(pnum));
	printf("size of is character %d\n",sizeof(pch));
	printf("size of is float %d\n",sizeof(pfnum));
	printf("size of is double %d\n",sizeof(pdnum));
	printf("size of is long %d\n",sizeof(plnum));
}
